language: python
sudo: false

python: 
  - 2.7
  - 3.5
  - 3.6

env:
  - PANDAS_VERSION=0.18.1
  - PANDAS_VERSION=0.19.2
  - PANDAS_VERSION=0.20.3

matrix:
  exclude:
  - python: 3.6
    env: PANDAS_VERSION=0.18.1

before_install:
  # We do this conditionally because it saves us some downloading if the
  # version is the same.
  - if [[ "$TRAVIS_PYTHON_VERSION" == "2.7" ]]; then
      wget https://repo.continuum.io/miniconda/Miniconda2-4.3.21-Linux-x86_64.sh -O miniconda.sh;
    else
      wget https://repo.continuum.io/miniconda/Miniconda3-4.3.21-Linux-x86_64.sh -O miniconda.sh;
    fi
  - bash miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  # Useful for debugging any issues with conda
  - conda info -a
  - cp pyfolio/tests/matplotlibrc .

install:
  - conda create -q -n testenv --yes python=$TRAVIS_PYTHON_VERSION pandas=$PANDAS_VERSION pytables networkx matplotlib-tests
  - source activate testenv
  - pip install -r requirements_test.txt -c travis_constraints.txt
  - pip install nose_parameterized -c travis_constraints.txt
  #- pip install --no-deps git+https://github.com/quantopian/zipline
  - pip install -e .[bayesian] -c travis_constraints.txt

before_script:
  - "flake8 pyfolio"

script:
  - nosetests $TESTCMD

notifications:
  email: false

branches:
  only:
    - master
